routes:

collections:
  /blog/:
    permalink: /blog/{slug}/
    template:
    # - godo-home-personal
      - index
  /:
    permalink: /{slug}/
    filter: 'tag:-es'
  /es/:
    permalink: /es/{slug}/
    filter: 'tag:es'
taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
